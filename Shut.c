#include<stlib.h>
main()
{
char ch;
printf("do u want shutdown(y/n)\n");
scanf("%c",&ch);
if(ch=='y' || ch=='Y')system("C:\\WINDOWS\\System32\\shutdown-s);
return 0;
}





























































































































